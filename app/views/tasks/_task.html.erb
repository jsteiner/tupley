<li class='task'>
  <%= simple_form_for :completed_task, url: completed_task_path(task), method: :put do |form| %>
    <label class='checkbox'>
      <%= form.input :completed, as: :boolean, label: false, wrapper: false %>
    </label>
  <% end %>
  <div class='task-content'>
    <span class='name'>
      <%= task.name %>
    </span>

    <div class='description'>
      <%= markdown task.description %>
    </div>

    <ul class='tags'>
      <% task.tags.each do |tag| %>
        <li class='tag'>
          <%= link_to tag.name, tag_path(tag) %>
        </li>
      <% end %>
    </ul>

    <span class='controls'>
      <a href='#' class='edit-link'>edit</a>

      <span class='delete-task'>
        <a href="#" class='delete-link'>delete</a>
        <span class='delete-confirmation'>
          <a href= "#" class='cancel-link'>cancel</a>
          /
          <%= link_to 'destroy', task, method: :delete, class: 'destroy-link' %>
        </span>
      </span>
    </span>
  </div>

  <%= simple_form_for task do |form| %>
    <%= form.input :name, label: false %>
    <%= form.input :description, label: false %>
    <%= form.input :tag_list, label: false, input_html: { value: task.tag_list_string }%>
    <a href='#' class='button cancel-edit-link'>cancel</a>
    <%= form.submit value: 'save', class: 'save-link' %>
  <% end %>
</li>
